/* const persons = ['Marco', 'Kim', 'Simon'];

persons.forEach(function (person, index) {
    console.log(`${index} : ${person}`);
});




const users = [
    { id: 1, name: 'john' },
    { id: 2, name: 'Marko' },
    { id: 3, name: 'Wick' }
]

const ids = users.map(function (user) {
    return user.id;
})

console.log(ids) */